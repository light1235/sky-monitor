import React from 'react';
import './index.scss';

const BlockSlat = ({text}) => {
     return (
          <div>
               <div className="block-slat">{text}</div>
          </div>
     );
};

export default BlockSlat;
